---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: ca-centos-6
    image: centos:6
provisioner:
  name: ansible
  inventory:
    group_vars:
      all:
        httpd_ignore_docker: no
        ca_ignore_docker: no
  lint:
    name: ansible-lint
scenario:
  name: centos-6
